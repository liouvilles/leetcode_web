{"id":2846,"slug":"minimum-edge-weight-equilibrium-queries-in-a-tree","titleEn":"Minimum Edge Weight Equilibrium Queries in a Tree","difficulty":"困难","category":"动态规划","officialTags":["数组","动态规划","位运算","树","深度优先搜索"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/minimum-edge-weight-equilibrium-queries-in-a-tree/","title":"边权重均衡查询的最小操作次数","studyTags":["数组","树","LCA","倍增","前缀计数"],"summary":"树边权一到二十六，每个查询可修改路径边权，求让路径所有边权相同的最少修改次数。","insight":"答案是路径边数减去路径上出现最多的权重次数。根化树后保存每个节点根路径的二十六维权重前缀计数，并用倍增求 LCA；u、v 路径某权重次数由两个前缀减两倍 LCA 前缀得到。","pitfalls":["查询路径不含 LCA 上方边","边权下标要减一","路径长度由深度计算","LCA 与前缀表可复用所有查询"],"solutions":[{"id":"lca-and-root-path-weight-prefix","title":"LCA 加根路径边权前缀","kind":"最优","idea":"每次查询常数二十六维统计路径众数。","steps":["构建无向邻接表","从根零 BFS 得到 depth、直接父节点和 prefix[node][26]","构建父节点倍增表","每个查询求 ancestor=LCA(u,v)","length=depth[u]+depth[v]-2*depth[ancestor]","枚举权重计算路径频次并取 maximum","answer=length-maximum"],"complexity":{"time":"O((n+q) log n+26q)","space":"O(n log n+26n)"},"code":{"java":"class Solution {\n    private int lca(int a,int b,int[][] up,int[] depth){\n        if(depth[a]<depth[b]){\n            int swap=a;\n            a=b;\n            b=swap;\n        }\n        int difference=depth[a]-depth[b];\n        for(int bit=0;bit<up.length;bit++)if(((difference>>bit)&1)==1)a=up[bit][a];\n        if(a==b)return a;\n        for(int bit=up.length-1;bit>=0;bit--)if(up[bit][a]!=up[bit][b]){\n            a=up[bit][a];\n            b=up[bit][b];\n        }\n        return up[0][a];\n    }\n    public int[] minOperationsQueries(int n,int[][] edges,int[][] queries){\n        List<List<int[]>> graph=new ArrayList<>();\n        for(int i=0;i<n;i++)graph.add(new ArrayList<>());\n        for(int[] edge:edges){\n            graph.get(edge[0]).add(new int[]{\n                edge[1],edge[2]-1\n            });\n            graph.get(edge[1]).add(new int[]{\n                edge[0],edge[2]-1\n            });\n        }\n        int levels=1;\n        while((1<<levels)<=n)levels++;\n        int[][] up=new int[levels][n],prefix=new int[n][26];\n        int[] depth=new int[n];\n        Arrays.fill(up[0],-1);\n        up[0][0]=0;\n        Deque<Integer> queue=new ArrayDeque<>();\n        queue.add(0);\n        while(!queue.isEmpty()){\n            int node=queue.removeFirst();\n            for(int[] edge:graph.get(node)){\n                int next=edge[0];\n                if(next==up[0][node])continue;\n                up[0][next]=node;\n                depth[next]=depth[node]+1;\n                prefix[next]=prefix[node].clone();\n                prefix[next][edge[1]]++;\n                queue.addLast(next);\n            }\n        }\n        for(int bit=1;bit<levels;bit++)for(int node=0;node<n;node++)up[bit][node]=up[bit-1][up[bit-1][node]];\n        int[] answer=new int[queries.length];\n        for(int i=0;i<queries.length;i++){\n            int u=queries[i][0],v=queries[i][1],ancestor=lca(u,v,up,depth);\n            int length=depth[u]+depth[v]-2*depth[ancestor],maximum=0;\n            for(int weight=0;weight<26;weight++)maximum=Math.max(maximum,prefix[u][weight]+prefix[v][weight]-2*prefix[ancestor][weight]);\n            answer[i]=length-maximum;\n        }\n        return answer;\n    }\n}\n","kotlin":"class Solution {\n    fun minOperationsQueries(n:Int,edges:Array<IntArray>,queries:Array<IntArray>):IntArray{\n        val graph=List(n){\n            mutableListOf<IntArray>()\n        };\n        for(edge in edges){\n            graph[edge[0]].add(intArrayOf(edge[1],edge[2]-1));\n            graph[edge[1]].add(intArrayOf(edge[0],edge[2]-1))\n        };\n        var levels=1;\n        while((1 shl levels)<=n)levels++;\n        val up=Array(levels){\n            IntArray(n)\n        };\n        val prefix=Array(n){\n            IntArray(26)\n        };\n        val depth=IntArray(n);\n        java.util.Arrays.fill(up[0],-1);\n        up[0][0]=0;\n        val queue=java.util.ArrayDeque<Int>();\n        queue.addLast(0);\n        while(!queue.isEmpty()){\n            val node=queue.pollFirst();\n            for(edge in graph[node]){\n                val next=edge[0];\n                if(next==up[0][node])continue;\n                up[0][next]=node;\n                depth[next]=depth[node]+1;\n                prefix[next]=prefix[node].clone();\n                prefix[next][edge[1]]++;\n                queue.addLast(next)\n            }\n        };\n        for(bit in 1 until levels)for(node in 0 until n)up[bit][node]=up[bit-1][up[bit-1][node]];\n        fun lca(first:Int,second:Int):Int{\n            var a=first;\n            var b=second;\n            if(depth[a]<depth[b]){\n                val swap=a;\n                a=b;\n                b=swap\n            };\n            val difference=depth[a]-depth[b];\n            for(bit in 0 until levels)if((difference shr bit and 1)==1)a=up[bit][a];\n            if(a==b)return a;\n            for(bit in levels-1 downTo 0)if(up[bit][a]!=up[bit][b]){\n                a=up[bit][a];\n                b=up[bit][b]\n            };\n            return up[0][a]\n        };\n        return IntArray(queries.size){\n            i->val u=queries[i][0];\n            val v=queries[i][1];\n            val ancestor=lca(u,v);\n            val length=depth[u]+depth[v]-2*depth[ancestor];\n            var maximum=0;\n            for(weight in 0 until 26)maximum=maxOf(maximum,prefix[u][weight]+prefix[v][weight]-2*prefix[ancestor][weight]);\n            length-maximum\n        }\n    }\n}\n","cpp":"class Solution {\n    public:vector<int> minOperationsQueries(int n,vector<vector<int>>& edges,vector<vector<int>>& queries){\n        vector<vector<pair<int,int>>> graph(n);\n        for(auto& edge:edges){\n            graph[edge[0]].push_back({\n                edge[1],edge[2]-1\n            });\n            graph[edge[1]].push_back({\n                edge[0],edge[2]-1\n            });\n        }\n        int levels=1;\n        while((1<<levels)<=n)++levels;\n        vector up(levels,vector<int>(n));\n        vector<array<int,26>> prefix(n);\n        vector<int> depth(n);\n        fill(up[0].begin(),up[0].end(),-1);\n        up[0][0]=0;\n        queue<int> pending;\n        pending.push(0);\n        while(!pending.empty()){\n            int node=pending.front();\n            pending.pop();\n            for(auto [next,weight]:graph[node]){\n                if(next==up[0][node])continue;\n                up[0][next]=node;\n                depth[next]=depth[node]+1;\n                prefix[next]=prefix[node];\n                ++prefix[next][weight];\n                pending.push(next);\n            }\n        }\n        for(int bit=1;bit<levels;++bit)for(int node=0;node<n;++node)up[bit][node]=up[bit-1][up[bit-1][node]];\n        auto lca=[&](int a,int b){\n            if(depth[a]<depth[b])swap(a,b);\n            int difference=depth[a]-depth[b];\n            for(int bit=0;bit<levels;++bit)if(difference>>bit&1)a=up[bit][a];\n            if(a==b)return a;\n            for(int bit=levels-1;bit>=0;--bit)if(up[bit][a]!=up[bit][b]){\n                a=up[bit][a];\n                b=up[bit][b];\n            }\n            return up[0][a];\n        };\n        vector<int> answer;\n        for(auto& query:queries){\n            int u=query[0],v=query[1],ancestor=lca(u,v),length=depth[u]+depth[v]-2*depth[ancestor],maximum=0;\n            for(int weight=0;weight<26;++weight)maximum=max(maximum,prefix[u][weight]+prefix[v][weight]-2*prefix[ancestor][weight]);\n            answer.push_back(length-maximum);\n        }\n        return answer;\n    }\n};\n"}}]}
