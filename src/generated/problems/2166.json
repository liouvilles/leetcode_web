{"id":2166,"slug":"design-bitset","titleEn":"Design Bitset","difficulty":"中等","category":"字符串","officialTags":["数组","哈希表","字符串","设计"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/design-bitset/","title":"设计位集","studyTags":["数组","字符串","设计"],"summary":"设计固定大小位集，支持置一、置零、整体翻转、全一判断、任一为一、统计一数和输出字符串。","insight":"保存底层位与全局 flipped 标志，逻辑位等于底层位异或 flipped。整体翻转只切换标志并令 ones=size-ones；单点更新按逻辑值判断后写入对应底层值。","pitfalls":["重复 fix 或 unfix 不改变计数","flip 后单点写入要考虑懒标志","toString 输出逻辑位"],"solutions":[{"id":"lazy-global-flip-bitset","title":"全局懒翻转位集","kind":"最优","idea":"一个翻转标志把 O(n) flip 降为 O(1)。","steps":["bits 保存未应用全局 flip 的布尔值，ones 保存逻辑一数量","fix/unfix 先求 bits[idx] xor flipped，仅状态变化时更新底层位和 ones","flip 切换标志并 ones=size-ones；查询直接使用 ones；toString 逐位异或输出"],"complexity":{"time":"O(1) 更新与查询，O(n) toString","space":"O(n)"},"code":{"java":"class Bitset {\n    boolean[] bits;\n    boolean flipped;\n    int ones;\n    public Bitset(int size){\n        bits=new boolean[size];\n    }\n    public void fix(int idx){\n        if(!(bits[idx]^flipped)){\n            bits[idx]=!flipped;\n            ones++;\n        }\n    }\n    public void unfix(int idx){\n        if(bits[idx]^flipped){\n            bits[idx]=flipped;\n            ones--;\n        }\n    }\n    public void flip(){\n        flipped=!flipped;\n        ones=bits.length-ones;\n    }\n    public boolean all(){\n        return ones==bits.length;\n    }\n    public boolean one(){\n        return ones>0;\n    }\n    public int count(){\n        return ones;\n    }\n    public String toString(){\n        StringBuilder value=new StringBuilder();\n        for(boolean bit:bits)value.append(bit^flipped?'1':'0');\n        return value.toString();\n    }\n}\n","kotlin":"class Bitset(private val size:Int){\n    private val bits=BooleanArray(size);\n    private var flipped=false;\n    private var ones=0;\n    fun fix(idx:Int){\n        if(!(bits[idx] xor flipped)){\n            bits[idx]=!flipped;\n            ones++\n        }\n    }\n    fun unfix(idx:Int){\n        if(bits[idx] xor flipped){\n            bits[idx]=flipped;\n            ones--\n        }\n    }\n    fun flip(){\n        flipped=!flipped;\n        ones=size-ones\n    }\n    fun all()=ones==size;\n    fun one()=ones>0;\n    fun count()=ones;\n    override fun toString():String=buildString{\n        for(bit in bits)append(if(bit xor flipped)'1' else '0')\n    }\n}\n","cpp":"class Bitset {\n    vector<char> bits;\n    bool flipped=false;\n    int ones=0;\n    public:Bitset(int size):bits(size){\n    }\n    void fix(int idx){\n        if(!(bits[idx]^flipped)){\n            bits[idx]=!flipped;\n            ++ones;\n        }\n    }\n    void unfix(int idx){\n        if(bits[idx]^flipped){\n            bits[idx]=flipped;\n            --ones;\n        }\n    }\n    void flip(){\n        flipped=!flipped;\n        ones=bits.size()-ones;\n    }\n    bool all(){\n        return ones==(int)bits.size();\n    }\n    bool one(){\n        return ones>0;\n    }\n    int count(){\n        return ones;\n    }\n    string toString(){\n        string value;\n        for(bool bit:bits)value+=bit^flipped?'1':'0';\n        return value;\n    }\n};\n"}}]}
