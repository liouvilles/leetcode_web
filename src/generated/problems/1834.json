{"id":1834,"slug":"single-threaded-cpu","titleEn":"Single-Threaded CPU","difficulty":"中等","category":"数据结构与设计","officialTags":["数组","排序","堆（优先队列）"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/single-threaded-cpu/","title":"单线程 CPU","studyTags":["数组","排序","堆（优先队列）"],"summary":"任务有入队时间和处理时长；CPU 空闲时选择已入队处理中时长最短、再下标最小者，返回处理顺序。","insight":"任务按入队时间排序，用小根堆维护当前可执行任务并按 [processing,index] 排序。堆空时把当前时间跳到下一任务入队时刻。","pitfalls":["时间累加使用 long","同时可用时按处理时长后下标排序","堆空不能逐时刻递增"],"solutions":[{"id":"enqueue-sort-ready-heap","title":"入队排序与就绪堆","kind":"最优","idea":"按事件时间把任务加入符合调度规则的优先队列。","steps":["构造 [enqueue,processing,index] 并按 enqueue 排序","while 未完成：堆空时 time=max(time,next.enqueue)；把 enqueue<=time 全部入堆","弹出 processing 最小任务，记录 index，time+=processing"],"complexity":{"time":"O(n log n)","space":"O(n)"},"code":{"java":"class Solution {\n    public int[] getOrder(int[][] tasks){\n        int n=tasks.length;\n        int[][] indexed=new int[n][3];\n        for(int i=0;i<n;i++)indexed[i]=new int[]{\n            tasks[i][0],tasks[i][1],i\n        };\n        Arrays.sort(indexed,Comparator.comparingInt(a->a[0]));\n        PriorityQueue<int[]> ready=new PriorityQueue<>((a,b)->a[1]!=b[1]?a[1]-b[1]:a[2]-b[2]);\n        int[] answer=new int[n];\n        int next=0,done=0;\n        long time=0;\n        while(done<n){\n            if(ready.isEmpty())time=Math.max(time,indexed[next][0]);\n            while(next<n&&indexed[next][0]<=time)ready.offer(indexed[next++]);\n            int[] task=ready.poll();\n            answer[done++]=task[2];\n            time+=task[1];\n        }\n        return answer;\n    }\n}\n","kotlin":"class Solution {\n    fun getOrder(tasks:Array<IntArray>):IntArray{\n        val indexed=tasks.indices.map{\n            intArrayOf(tasks[it][0],tasks[it][1],it)\n        }.sortedBy{\n            it[0]\n        };\n        val ready=java.util.PriorityQueue<IntArray>{\n            a,b->if(a[1]!=b[1])a[1]-b[1] else a[2]-b[2]\n        };\n        val answer=IntArray(tasks.size);\n        var next=0;\n        var done=0;\n        var time=0L;\n        while(done<tasks.size){\n            if(ready.isEmpty())time=maxOf(time,indexed[next][0].toLong());\n            while(next<tasks.size&&indexed[next][0]<=time)ready.add(indexed[next++]);\n            val task=ready.remove();\n            answer[done++]=task[2];\n            time+=task[1]\n        };\n        return answer\n    }\n}\n","cpp":"class Solution {\n    public: vector<int> getOrder(vector<vector<int>>& tasks){\n        vector<array<int,3>> indexed;\n        for(int i=0;i<(int)tasks.size();++i)indexed.push_back({\n            tasks[i][0],tasks[i][1],i\n        });\n        sort(indexed.begin(),indexed.end());\n        priority_queue<pair<int,int>,vector<pair<int,int>>,greater<pair<int,int>>> ready;\n        vector<int> answer;\n        int next=0;\n        long long time=0;\n        while(answer.size()<tasks.size()){\n            if(ready.empty())time=max(time,1LL*indexed[next][0]);\n            while(next<(int)tasks.size()&&indexed[next][0]<=time){\n                ready.push({\n                    indexed[next][1],indexed[next][2]\n                });\n                ++next;\n            }\n            auto [duration,index]=ready.top();\n            ready.pop();\n            answer.push_back(index);\n            time+=duration;\n        }\n        return answer;\n    }\n};\n"}}]}
