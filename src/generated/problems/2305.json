{"id":2305,"slug":"fair-distribution-of-cookies","titleEn":"Fair Distribution of Cookies","difficulty":"中等","category":"动态规划","officialTags":["数组","动态规划","回溯","位运算","状态压缩"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/fair-distribution-of-cookies/","title":"公平分发饼干","studyTags":["数组","回溯","状态压缩"],"summary":"把每袋饼干完整分给 k 个孩子，孩子的不公平程度为所得总数，求所有孩子最大所得数的最小可能值。","insight":"按袋大小降序回溯分配，尽早形成强剪枝。若当前最大值已不小于最优答案则停止；相同负载孩子等价，空孩子也只试一个。","pitfalls":["每袋不能拆分","允许某孩子暂时为空","目标是最小化最大负载而非负载差"],"solutions":[{"id":"descending-backtracking-with-symmetry-pruning","title":"降序回溯与对称剪枝","kind":"最优","idea":"优先放大袋并跳过等价孩子状态。","steps":["cookies 降序，loads[k]=0，best=∞","递归处理第 index 袋；若 currentMax>=best 剪枝","对未出现过的 loads[i] 尝试放入，递归后撤销；原负载为零时尝试一次即停止","分完更新 best"],"complexity":{"time":"O(k^n)，剪枝显著减少","space":"O(k+n)"},"code":{"java":"class Solution {\n    int[] cookies,loads;\n    int best;\n    private void search(int index,int current){\n        if(current>=best)return;\n        if(index==cookies.length){\n            best=current;\n            return;\n        }\n        Set<Integer> seen=new HashSet<>();\n        for(int i=0;i<loads.length;i++){\n            if(!seen.add(loads[i]))continue;\n            int before=loads[i];\n            loads[i]+=cookies[index];\n            search(index+1,Math.max(current,loads[i]));\n            loads[i]=before;\n            if(before==0)break;\n        }\n    }\n    public int distributeCookies(int[] cookies,int k){\n        Arrays.sort(cookies);\n        this.cookies=new int[cookies.length];\n        for(int i=0;i<cookies.length;i++)this.cookies[i]=cookies[cookies.length-1-i];\n        loads=new int[k];\n        best=Integer.MAX_VALUE;\n        search(0,0);\n        return best;\n    }\n}\n","kotlin":"class Solution {\n    fun distributeCookies(cookies:IntArray,k:Int):Int{\n        val ordered=cookies.sortedArrayDescending();\n        val loads=IntArray(k);\n        var best=Int.MAX_VALUE;\n        fun search(index:Int,current:Int){\n            if(current>=best)return;\n            if(index==ordered.size){\n                best=current;\n                return\n            };\n            val seen=HashSet<Int>();\n            for(i in loads.indices){\n                if(!seen.add(loads[i]))continue;\n                val before=loads[i];\n                loads[i]+=ordered[index];\n                search(index+1,maxOf(current,loads[i]));\n                loads[i]=before;\n                if(before==0)break\n            }\n        };\n        search(0,0);\n        return best\n    }\n}\n","cpp":"class Solution {\n    vector<int> cookies,loads;\n    int best=INT_MAX;\n    void search(int index,int current){\n        if(current>=best)return;\n        if(index==(int)cookies.size()){\n            best=current;\n            return;\n        }\n        unordered_set<int> seen;\n        for(int i=0;i<(int)loads.size();++i){\n            if(!seen.insert(loads[i]).second)continue;\n            int before=loads[i];\n            loads[i]+=cookies[index];\n            search(index+1,max(current,loads[i]));\n            loads[i]=before;\n            if(before==0)break;\n        }\n    }\n    public:int distributeCookies(vector<int>& values,int k){\n        cookies=values;\n        sort(cookies.rbegin(),cookies.rend());\n        loads.assign(k,0);\n        search(0,0);\n        return best;\n    }\n};\n"}}]}
