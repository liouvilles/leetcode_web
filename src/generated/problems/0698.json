{"id":698,"slug":"partition-to-k-equal-sum-subsets","titleEn":"Partition to K Equal Sum Subsets","difficulty":"中等","category":"动态规划","officialTags":["数组","动态规划","回溯","位运算","记忆化搜索","状态压缩"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/partition-to-k-equal-sum-subsets/","title":"划分为 k 个相等的子集","studyTags":["数组","回溯","剪枝"],"summary":"判断正整数数组能否分成 k 个非空子集，使每个子集元素和都相等。","insight":"总和必须能被 k 整除；把数字降序放入 k 个容量为 target 的桶，先放大数可更早触发剪枝。","pitfalls":["最大元素不能超过目标和","相同桶当前和只需尝试一次","空桶尝试失败后无需再尝试其他等价空桶"],"solutions":[{"id":"descending-bucket-backtracking","title":"降序元素填桶回溯","kind":"最优","idea":"逐个把数字放入可容纳的桶，并跳过对称状态。","steps":["计算 target=sum/k 并检查整除，数组降序","递归 index：尝试 buckets[bucket]+value<=target 的桶","同一层跳过相同桶和；放入后递归，失败回退，空桶失败后 break"],"complexity":{"time":"O(k^n)","space":"O(k+n)"},"code":{"java":"class Solution {\n    public boolean canPartitionKSubsets(int[] nums,int k){\n        int sum=Arrays.stream(nums).sum();\n        if(sum%k!=0)return false;\n        int target=sum/k;\n        Arrays.sort(nums);\n        if(nums[nums.length-1]>target)return false;\n        int[] buckets=new int[k];\n        return place(nums,nums.length-1,buckets,target);\n    }\n    private boolean place(int[] nums,int index,int[] buckets,int target){\n        if(index<0)return true;\n        int value=nums[index],previous=-1;\n        for(int bucket=0;bucket<buckets.length;bucket++){\n            if(buckets[bucket]==previous||buckets[bucket]+value>target)continue;\n            previous=buckets[bucket];\n            buckets[bucket]+=value;\n            if(place(nums,index-1,buckets,target))return true;\n            buckets[bucket]-=value;\n            if(buckets[bucket]==0)break;\n        }\n        return false;\n    }\n}\n","kotlin":"class Solution {\n    fun canPartitionKSubsets(nums:IntArray,k:Int):Boolean{\n        val sum=nums.sum();\n        if(sum%k!=0)return false;\n        val target=sum/k;\n        nums.sort();\n        if(nums.last()>target)return false;\n        val buckets=IntArray(k);\n        fun place(index:Int):Boolean{\n            if(index<0)return true;\n            val value=nums[index];\n            var previous=-1;\n            for(bucket in buckets.indices){\n                if(buckets[bucket]==previous||buckets[bucket]+value>target)continue;\n                previous=buckets[bucket];\n                buckets[bucket]+=value;\n                if(place(index-1))return true;\n                buckets[bucket]-=value;\n                if(buckets[bucket]==0)break\n            };\n            return false\n        };\n        return place(nums.lastIndex)\n    }\n}\n","cpp":"class Solution {\n    bool place(vector<int>& nums,int index,vector<int>& buckets,int target){\n        if(index<0)return true;\n        int value=nums[index],previous=-1;\n        for(int bucket=0;bucket<(int)buckets.size();++bucket){\n            if(buckets[bucket]==previous||buckets[bucket]+value>target)continue;\n            previous=buckets[bucket];\n            buckets[bucket]+=value;\n            if(place(nums,index-1,buckets,target))return true;\n            buckets[bucket]-=value;\n            if(buckets[bucket]==0)break;\n        }\n        return false;\n    }\n    public:bool canPartitionKSubsets(vector<int>& nums,int k){\n        int sum=accumulate(nums.begin(),nums.end(),0);\n        if(sum%k)return false;\n        int target=sum/k;\n        sort(nums.begin(),nums.end());\n        if(nums.back()>target)return false;\n        vector<int> buckets(k);\n        return place(nums,nums.size()-1,buckets,target);\n    }\n};\n"}}]}
