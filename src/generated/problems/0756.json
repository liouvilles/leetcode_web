{"id":756,"slug":"pyramid-transition-matrix","titleEn":"Pyramid Transition Matrix","difficulty":"中等","category":"字符串","officialTags":["哈希表","字符串","回溯","位运算"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/pyramid-transition-matrix/","title":"金字塔转换矩阵","studyTags":["哈希表","字符串","回溯"],"summary":"底层字符串上方每个字符由下方相邻字符对按 allowed 三元组生成，判断能否逐层构造到单字符顶端。","insight":"把每个二字符底边映射到所有可选顶字符；对当前行从左到右回溯生成下一行，完成后递归处理下一层。","pitfalls":["同一字符对可能有多个顶字符选择","某位置没有允许转换时当前分支立即失败","长度一的底层已经成功"],"solutions":[{"id":"row-by-row-backtracking","title":"逐层字符串回溯生成","kind":"最优","idea":"预处理相邻对到顶字符列表，并搜索所有下一层组合。","steps":["建立 pair→characters 映射","solve(row) 长度一返回真，否则 build(position,next)","build 对 row[position..position+1] 的所有候选追加字符；完成 next 时递归 solve(next)，成功立即返回"],"complexity":{"time":"O(branches^(n²))","space":"O(n²)"},"code":{"java":"class Solution {\n    private final Map<String,List<Character>> choices=new HashMap<>();\n    public boolean pyramidTransition(String bottom,List<String> allowed){\n        for(String rule:allowed)choices.computeIfAbsent(rule.substring(0,2),key->new ArrayList<>()).add(rule.charAt(2));\n        return solve(bottom);\n    }\n    private boolean solve(String row){\n        if(row.length()==1)return true;\n        return build(row,0,new StringBuilder());\n    }\n    private boolean build(String row,int position,StringBuilder next){\n        if(position==row.length()-1)return solve(next.toString());\n        List<Character> options=choices.get(row.substring(position,position+2));\n        if(options==null)return false;\n        for(char c:options){\n            next.append(c);\n            if(build(row,position+1,next))return true;\n            next.setLength(next.length()-1);\n        }\n        return false;\n    }\n}\n","kotlin":"class Solution {\n    private val choices=hashMapOf<String,MutableList<Char>>();\n    fun pyramidTransition(bottom:String,allowed:List<String>):Boolean{\n        for(rule in allowed)choices.getOrPut(rule.substring(0,2)){\n            mutableListOf()\n        }.add(rule[2]);\n        return solve(bottom)\n    };\n    private fun solve(row:String):Boolean{\n        if(row.length==1)return true;\n        return build(row,0,StringBuilder())\n    };\n    private fun build(row:String,position:Int,next:StringBuilder):Boolean{\n        if(position==row.length-1)return solve(next.toString());\n        val options=choices[row.substring(position,position+2)]?:return false;\n        for(c in options){\n            next.append(c);\n            if(build(row,position+1,next))return true;\n            next.setLength(next.length-1)\n        };\n        return false\n    }\n}\n","cpp":"class Solution {\n    unordered_map<string,vector<char>> choices;\n    bool solve(string row){\n        if(row.size()==1)return true;\n        string next;\n        return build(row,0,next);\n    }\n    bool build(string& row,int position,string& next){\n        if(position==(int)row.size()-1)return solve(next);\n        auto it=choices.find(row.substr(position,2));\n        if(it==choices.end())return false;\n        for(char c:it->second){\n            next+=c;\n            if(build(row,position+1,next))return true;\n            next.pop_back();\n        }\n        return false;\n    }\n    public:bool pyramidTransition(string bottom,vector<string>& allowed){\n        for(string& rule:allowed)choices[rule.substr(0,2)].push_back(rule[2]);\n        return solve(bottom);\n    }\n};\n"}}]}
