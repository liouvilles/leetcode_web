{"id":2910,"slug":"minimum-number-of-groups-to-create-a-valid-assignment","titleEn":"Minimum Number of Groups to Create a Valid Assignment","difficulty":"中等","category":"数组与矩阵","officialTags":["数组","哈希表","贪心"],"paidOnly":false,"sourceUrl":"https://leetcode.cn/problems/minimum-number-of-groups-to-create-a-valid-assignment/","title":"合法分组的最少组数","studyTags":["数组","哈希表","贪心"],"summary":"把相同数值的球分别装入若干组，每组内数值必须相同，且任意两组的大小之差至多为一，求所需最少组数。","insight":"设全局较小组的大小为 size，则所有组只能有 size 或 size+1 个球。频次 count 能拆成这两种大小，当且仅当 count/size 不小于 count%size；可行时该频次的最少组数为 ceil(count/(size+1))。从最小频次向下枚举 size，第一个全局可行值最大，因此总组数最少。","pitfalls":["同一数值的多个球允许被拆到不同组","所有数值必须共享同一对候选组大小","只计算向上取整而不检查可表示条件会接受非法频次","应从较大 size 向下找，首个可行方案组数最少"],"solutions":[{"id":"descending-common-group-size","title":"倒序枚举公共小组大小","kind":"最优","idea":"按频次判断能否由 size 与 size+1 组成。","steps":["用哈希表统计每个球值的频次","取最小频次作为 size 的枚举上界","对每个频次计算 quotient=count/size 与 remainder=count%size","若 quotient<remainder，则该 size 不可行","否则累加 (count+size)/(size+1)","返回第一个对所有频次都可行的总组数"],"complexity":{"time":"O(n+d*fmin)=O(n)","space":"O(d)"},"code":{"java":"class Solution {\n    public int minGroupsForValidAssignment(int[] balls){\n        Map<Integer,Integer> frequencies=new HashMap<>();\n        for(int ball:balls)frequencies.put(ball,frequencies.getOrDefault(ball,0)+1);\n        int smallest=balls.length;\n        for(int frequency:frequencies.values())smallest=Math.min(smallest,frequency);\n        for(int size=smallest;size>=1;size--){\n            int groups=0;\n            boolean feasible=true;\n            for(int frequency:frequencies.values()){\n                int quotient=frequency/size,remainder=frequency%size;\n                if(quotient<remainder){\n                    feasible=false;\n                    break;\n                }\n                groups+=(frequency+size)/(size+1);\n            }\n            if(feasible)return groups;\n        }\n        return balls.length;\n    }\n}\n","kotlin":"class Solution {\n    fun minGroupsForValidAssignment(balls:IntArray):Int{\n        val frequencies=HashMap<Int,Int>();\n        for(ball in balls)frequencies[ball]=(frequencies[ball]?:0)+1;\n        var smallest=balls.size;\n        for(frequency in frequencies.values)smallest=minOf(smallest,frequency);\n        for(size in smallest downTo 1){\n            var groups=0;\n            var feasible=true;\n            for(frequency in frequencies.values){\n                val quotient=frequency/size;\n                val remainder=frequency%size;\n                if(quotient<remainder){\n                    feasible=false;\n                    break\n                };\n                groups+=(frequency+size)/(size+1)\n            };\n            if(feasible)return groups\n        };\n        return balls.size\n    }\n}\n","cpp":"class Solution {\n    public:int minGroupsForValidAssignment(vector<int>& balls){\n        unordered_map<int,int> frequencies;\n        for(int ball:balls)++frequencies[ball];\n        int smallest=balls.size();\n        for(auto& entry:frequencies)smallest=min(smallest,entry.second);\n        for(int size=smallest;size>=1;--size){\n            int groups=0;\n            bool feasible=true;\n            for(auto& entry:frequencies){\n                int frequency=entry.second,quotient=frequency/size,remainder=frequency%size;\n                if(quotient<remainder){\n                    feasible=false;\n                    break;\n                }\n                groups+=(frequency+size)/(size+1);\n            }\n            if(feasible)return groups;\n        }\n        return balls.size();\n    }\n};\n"}}]}
