class Solution { fun maxCount(banned:IntArray,n:Int,maxSum:Int):Int{val blocked=banned.toHashSet();var sum=0;var count=0;for(value in 1..n)if(value !in blocked){if(sum+value>maxSum)break;sum+=value;count++};return count} }
