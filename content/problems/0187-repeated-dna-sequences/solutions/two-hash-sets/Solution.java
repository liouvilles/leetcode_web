class Solution { public List<String> findRepeatedDnaSequences(String s){ Set<String> seen=new HashSet<>(); Set<String> repeated=new LinkedHashSet<>(); for(int i=0;i+10<=s.length();i++){ String part=s.substring(i,i+10); if(!seen.add(part)) repeated.add(part); } return new ArrayList<>(repeated); } }
