class Solution { public String largestGoodInteger(String num){char best=0;for(int i=0;i+2<num.length();i++)if(num.charAt(i)==num.charAt(i+1)&&num.charAt(i)==num.charAt(i+2))best=(char)Math.max(best,num.charAt(i));return best==0?"":String.valueOf(best).repeat(3);} }
