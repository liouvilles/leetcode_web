const g=1755,t="closest-subsequence-sum",s="Closest Subsequence Sum",d="困难",n="动态规划",e=["数组","双指针","动态规划","位运算","折半搜索","排序","状态压缩"],c=!1,i="https://leetcode.cn/problems/closest-subsequence-sum/",a="最接近目标值的子序列和",r=["数组","双指针","二分查找","分治"],l="从数组任选元素形成子序列，使其和与 goal 的绝对差最小，返回最小差值。",u="把数组分成两半，分别枚举所有子集和。排序右半和后，对每个左半和二分寻找最接近 goal-leftSum 的右半和。",m=["空子序列合法，子集和零必须包含","元素可为负数","答案可能为零，可提前结束"],o=[{id:"meet-in-the-middle-subset-sums",title:"折半枚举子集和",kind:"最优",idea:"把指数规模从 2^n 降为两组 2^(n/2)。",steps:["生成左右半部全部 subset sums","右半 sums 排序","对每个 leftSum 二分 target=goal-leftSum，检查 lower_bound 位置及前一位置，更新最小绝对差"],complexity:{time:"O(2^(n/2) log 2^(n/2))",space:"O(2^(n/2))"},code:{java:`class Solution { private int[] sums(int[] nums,int start,int length){int[] sums=new int[1<<length];for(int mask=1;mask<sums.length;mask++){int bit=Integer.numberOfTrailingZeros(mask);sums[mask]=sums[mask^(1<<bit)]+nums[start+bit];}return sums;}public int minAbsDifference(int[] nums,int goal){int middle=nums.length/2;int[] left=sums(nums,0,middle),right=sums(nums,middle,nums.length-middle);Arrays.sort(right);int answer=Math.abs(goal);for(int value:left){int target=goal-value,index=Arrays.binarySearch(right,target);if(index>=0)return 0;index=-index-1;if(index<right.length)answer=Math.min(answer,Math.abs(value+right[index]-goal));if(index>0)answer=Math.min(answer,Math.abs(value+right[index-1]-goal));}return answer;} }
`,kotlin:`class Solution { private fun sums(nums:IntArray,start:Int,length:Int):IntArray{val sums=IntArray(1 shl length);for(mask in 1 until sums.size){val bit=Integer.numberOfTrailingZeros(mask);sums[mask]=sums[mask xor (1 shl bit)]+nums[start+bit]};return sums};fun minAbsDifference(nums:IntArray,goal:Int):Int{val middle=nums.size/2;val left=sums(nums,0,middle);val right=sums(nums,middle,nums.size-middle);right.sort();var answer=kotlin.math.abs(goal);for(value in left){val target=goal-value;var index=right.binarySearch(target);if(index>=0)return 0;index=-index-1;if(index<right.size)answer=minOf(answer,kotlin.math.abs(value+right[index]-goal));if(index>0)answer=minOf(answer,kotlin.math.abs(value+right[index-1]-goal))};return answer} }
`,cpp:`class Solution { vector<int> sums(vector<int>& nums,int start,int length){vector<int> values(1<<length);for(int mask=1;mask<(1<<length);++mask){int bit=__builtin_ctz(mask);values[mask]=values[mask^(1<<bit)]+nums[start+bit];}return values;}public:int minAbsDifference(vector<int>& nums,int goal){int middle=nums.size()/2;auto left=sums(nums,0,middle),right=sums(nums,middle,nums.size()-middle);sort(right.begin(),right.end());int answer=abs(goal);for(int value:left){int target=goal-value;auto it=lower_bound(right.begin(),right.end(),target);if(it!=right.end())answer=min(answer,abs(value+*it-goal));if(it!=right.begin())answer=min(answer,abs(value+*prev(it)-goal));}return answer;} };
`}}],h={id:1755,slug:t,titleEn:s,difficulty:"困难",category:n,officialTags:e,paidOnly:!1,sourceUrl:i,title:a,studyTags:r,summary:l,insight:u,pitfalls:m,solutions:o};export{n as category,h as default,d as difficulty,g as id,u as insight,e as officialTags,c as paidOnly,m as pitfalls,t as slug,o as solutions,i as sourceUrl,r as studyTags,l as summary,a as title,s as titleEn};
