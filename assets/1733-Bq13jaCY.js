const g=1733,n="minimum-number-of-people-to-teach",e="Minimum Number of People to Teach",l="中等",a="数组与矩阵",s=["数组","哈希表","贪心"],f=!1,i="https://leetcode.cn/problems/minimum-number-of-people-to-teach/",t="需要教语言的最少人数",r=["数组","哈希表"],o="选择一种语言教给若干用户，使每对朋友至少共享一种语言，求最少教学人数。",d="只有目前无法沟通的友谊需要处理，把这些友谊涉及的用户收集为候选集。选定语言后，候选集中已会该语言者无需教学，枚举语言取最小缺口。",c=["已经能沟通的朋友不限制所选教学语言","同一用户涉及多条坏友谊只计一次","所有被教用户必须学习同一种语言"],u=[{id:"bad-friendship-candidate-count",title:"坏友谊候选集计数",kind:"最优",idea:"先缩小到需要修复的用户，再枚举统一教学语言。",steps:["把每个用户语言存为集合","对 friendships 检查两人语言交集；无交集则把两人加入 candidates","对 language=1..n，统计 candidates 中不会该语言的人数并取最小"],complexity:{time:"O(total languages+friendships·L+n·users)",space:"O(total languages)"},code:{java:`class Solution { public int minimumTeachings(int n,int[][] languages,int[][] friendships){List<Set<Integer>> known=new ArrayList<>();for(int[] user:languages){Set<Integer> set=new HashSet<>();for(int language:user)set.add(language);known.add(set);}Set<Integer> candidates=new HashSet<>();for(int[] friendship:friendships){Set<Integer> first=known.get(friendship[0]-1),second=known.get(friendship[1]-1);boolean share=false;for(int language:first)if(second.contains(language)){share=true;break;}if(!share){candidates.add(friendship[0]-1);candidates.add(friendship[1]-1);}}int answer=candidates.size();for(int language=1;language<=n;language++){int teach=0;for(int user:candidates)if(!known.get(user).contains(language))teach++;answer=Math.min(answer,teach);}return answer;} }
`,kotlin:`class Solution { fun minimumTeachings(n:Int,languages:Array<IntArray>,friendships:Array<IntArray>):Int{val known=languages.map{it.toSet()};val candidates=mutableSetOf<Int>();for(friendship in friendships){val first=friendship[0]-1;val second=friendship[1]-1;if(known[first].intersect(known[second]).isEmpty()){candidates.add(first);candidates.add(second)}};var answer=candidates.size;for(language in 1..n)answer=minOf(answer,candidates.count{language !in known[it]});return answer} }
`,cpp:`class Solution { public: int minimumTeachings(int n,vector<vector<int>>& languages,vector<vector<int>>& friendships){vector<unordered_set<int>> known(languages.size());for(int user=0;user<(int)languages.size();++user)known[user].insert(languages[user].begin(),languages[user].end());unordered_set<int> candidates;for(auto& friendship:friendships){int first=friendship[0]-1,second=friendship[1]-1;bool share=false;for(int language:known[first])if(known[second].count(language)){share=true;break;}if(!share){candidates.insert(first);candidates.insert(second);}}int answer=candidates.size();for(int language=1;language<=n;++language){int teach=0;for(int user:candidates)teach+=!known[user].count(language);answer=min(answer,teach);}return answer;} };
`}}],h={id:1733,slug:n,titleEn:e,difficulty:"中等",category:a,officialTags:s,paidOnly:!1,sourceUrl:i,title:t,studyTags:r,summary:o,insight:d,pitfalls:c,solutions:u};export{a as category,h as default,l as difficulty,g as id,d as insight,s as officialTags,f as paidOnly,c as pitfalls,n as slug,u as solutions,i as sourceUrl,r as studyTags,o as summary,t as title,e as titleEn};
