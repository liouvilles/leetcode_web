const c=1573,t="number-of-ways-to-split-a-string",n="Number of Ways to Split a String",u="中等",g="字符串",e=["数学","字符串"],d=!1,s="https://leetcode.cn/problems/number-of-ways-to-split-a-string/",o="分割字符串的方案数",i=["数学","字符串"],r="把二进制字符串分成三个非空部分，使三部分一的数量相同，统计切分方案数取模。",a="总一数不能被三整除则无解。若全零，在 n-1 个间隙选两个；否则两处切分的可选间隙数分别是相邻目标一组之间的零数加一，二者相乘。",l=["三个部分都必须非空","全零情况单独使用组合数","切分间隙数由关键一的位置差决定"],p=[{id:"one-position-gaps",title:"一位置间隙计数",kind:"最优",idea:"定位每三分之一与下一组之间的可选切割区间。",steps:["收集所有一的位置 ones","ones.size%3!=0 返回 0；为零返回 C(n-1,2)","part=ones.size/3，firstGap=ones[part]-ones[part-1]，secondGap=ones[2part]-ones[2part-1]，乘积取模"],complexity:{time:"O(n)",space:"O(n)"},code:{java:`class Solution { public int numWays(String text){List<Integer> ones=new ArrayList<>();for(int i=0;i<text.length();i++)if(text.charAt(i)=='1')ones.add(i);long mod=1_000_000_007L;if(ones.size()%3!=0)return 0;if(ones.isEmpty())return (int)(((long)(text.length()-1)*(text.length()-2)/2)%mod);int part=ones.size()/3;long first=ones.get(part)-ones.get(part-1),second=ones.get(2*part)-ones.get(2*part-1);return (int)(first*second%mod);} }
`,kotlin:`class Solution { fun numWays(text:String):Int{val ones=text.indices.filter{text[it]=='1'};val mod=1_000_000_007L;if(ones.size%3!=0)return 0;if(ones.isEmpty())return (((text.length-1L)*(text.length-2)/2)%mod).toInt();val part=ones.size/3;return ((ones[part]-ones[part-1]).toLong()*(ones[2*part]-ones[2*part-1])%mod).toInt()} }
`,cpp:`class Solution { public: int numWays(string text){vector<int> ones;for(int i=0;i<(int)text.size();++i)if(text[i]=='1')ones.push_back(i);const long long mod=1000000007;if(ones.size()%3)return 0;if(ones.empty())return (text.size()-1LL)*(text.size()-2)/2%mod;int part=ones.size()/3;return 1LL*(ones[part]-ones[part-1])*(ones[2*part]-ones[2*part-1])%mod;} };
`}}],f={id:1573,slug:t,titleEn:n,difficulty:"中等",category:"字符串",officialTags:e,paidOnly:!1,sourceUrl:s,title:o,studyTags:i,summary:r,insight:a,pitfalls:l,solutions:p};export{g as category,f as default,u as difficulty,c as id,a as insight,e as officialTags,d as paidOnly,l as pitfalls,t as slug,p as solutions,s as sourceUrl,i as studyTags,r as summary,o as title,n as titleEn};
