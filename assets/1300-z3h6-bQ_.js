const m=1300,t="sum-of-mutated-array-closest-to-target",e="Sum of Mutated Array Closest to Target",g="中等",a="数组与矩阵",l=["数组","二分查找","排序"],d=!1,r="https://leetcode.cn/problems/sum-of-mutated-array-closest-to-target/",i="转变数组后最接近目标值的数组和",n=["数组","二分查找","单调性"],o="选择整数 value，把数组中大于 value 的元素改为 value，求使新数组和最接近 target 的 value；并列取较小值。",s="截断和随 value 单调不减。二分首个截断和不小于 target 的值，再比较它和前一个值的误差即可。",u=["答案可能等于零","并列误差必须选较小值","数组元素不超过十万，和用长整型更稳妥"],f=[{id:"binary-search-cap-and-neighbor",title:"二分截断值与相邻比较",kind:"最优",idea:"定位目标和两侧的两个整数候选。",steps:["left=0,right=max(arr)，二分首个 mutatedSum(value)>=target","计算 left 与 max(0,left-1) 的截断和误差","若较小候选误差不大于 left 则返回 left-1，否则返回 left"],complexity:{time:"O(n log maxValue)",space:"O(1)"},code:{java:`class Solution { public int findBestValue(int[] arr,int target){int left=0,right=0;for(int value:arr)right=Math.max(right,value);while(left<right){int middle=(left+right)/2;if(sum(arr,middle)>=target)right=middle;else left=middle+1;}int lower=Math.max(0,left-1);return Math.abs(sum(arr,lower)-target)<=Math.abs(sum(arr,left)-target)?lower:left;}private long sum(int[] arr,int cap){long total=0;for(int value:arr)total+=Math.min(value,cap);return total;} }
`,kotlin:`class Solution { fun findBestValue(arr:IntArray,target:Int):Int{var left=0;var right=arr.max();fun sum(cap:Int):Long{return arr.sumOf{minOf(it,cap).toLong()}};while(left<right){val middle=(left+right)/2;if(sum(middle)>=target)right=middle else left=middle+1};val lower=maxOf(0,left-1);return if(kotlin.math.abs(sum(lower)-target)<=kotlin.math.abs(sum(left)-target))lower else left} }
`,cpp:`class Solution { long long sum(vector<int>& arr,int cap){long long total=0;for(int value:arr)total+=min(value,cap);return total;}public:int findBestValue(vector<int>& arr,int target){int left=0,right=*max_element(arr.begin(),arr.end());while(left<right){int middle=(left+right)/2;if(sum(arr,middle)>=target)right=middle;else left=middle+1;}int lower=max(0,left-1);return llabs(sum(arr,lower)-target)<=llabs(sum(arr,left)-target)?lower:left;} };
`}}],c={id:1300,slug:t,titleEn:e,difficulty:"中等",category:a,officialTags:l,paidOnly:!1,sourceUrl:r,title:i,studyTags:n,summary:o,insight:s,pitfalls:u,solutions:f};export{a as category,c as default,g as difficulty,m as id,s as insight,l as officialTags,d as paidOnly,u as pitfalls,t as slug,f as solutions,r as sourceUrl,n as studyTags,o as summary,i as title,e as titleEn};
