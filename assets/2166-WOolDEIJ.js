const r=2166,i="design-bitset",e="Design Bitset",b="中等",a="字符串",t=["数组","哈希表","字符串","设计"],u=!1,n="https://leetcode.cn/problems/design-bitset/",s="设计位集",o=["数组","字符串","设计"],l="设计固定大小位集，支持置一、置零、整体翻转、全一判断、任一为一、统计一数和输出字符串。",p="保存底层位与全局 flipped 标志，逻辑位等于底层位异或 flipped。整体翻转只切换标志并令 ones=size-ones；单点更新按逻辑值判断后写入对应底层值。",d=["重复 fix 或 unfix 不改变计数","flip 后单点写入要考虑懒标志","toString 输出逻辑位"],f=[{id:"lazy-global-flip-bitset",title:"全局懒翻转位集",kind:"最优",idea:"一个翻转标志把 O(n) flip 降为 O(1)。",steps:["bits 保存未应用全局 flip 的布尔值，ones 保存逻辑一数量","fix/unfix 先求 bits[idx] xor flipped，仅状态变化时更新底层位和 ones","flip 切换标志并 ones=size-ones；查询直接使用 ones；toString 逐位异或输出"],complexity:{time:"O(1) 更新与查询，O(n) toString",space:"O(n)"},code:{java:`class Bitset { boolean[] bits;boolean flipped;int ones;public Bitset(int size){bits=new boolean[size];}public void fix(int idx){if(!(bits[idx]^flipped)){bits[idx]=!flipped;ones++;}}public void unfix(int idx){if(bits[idx]^flipped){bits[idx]=flipped;ones--;}}public void flip(){flipped=!flipped;ones=bits.length-ones;}public boolean all(){return ones==bits.length;}public boolean one(){return ones>0;}public int count(){return ones;}public String toString(){StringBuilder value=new StringBuilder();for(boolean bit:bits)value.append(bit^flipped?'1':'0');return value.toString();} }
`,kotlin:`class Bitset(private val size:Int){private val bits=BooleanArray(size);private var flipped=false;private var ones=0;fun fix(idx:Int){if(!(bits[idx] xor flipped)){bits[idx]=!flipped;ones++}}fun unfix(idx:Int){if(bits[idx] xor flipped){bits[idx]=flipped;ones--}}fun flip(){flipped=!flipped;ones=size-ones}fun all()=ones==size;fun one()=ones>0;fun count()=ones;override fun toString():String=buildString{for(bit in bits)append(if(bit xor flipped)'1' else '0')}}
`,cpp:`class Bitset { vector<char> bits;bool flipped=false;int ones=0;public:Bitset(int size):bits(size){}void fix(int idx){if(!(bits[idx]^flipped)){bits[idx]=!flipped;++ones;}}void unfix(int idx){if(bits[idx]^flipped){bits[idx]=flipped;--ones;}}void flip(){flipped=!flipped;ones=bits.size()-ones;}bool all(){return ones==(int)bits.size();}bool one(){return ones>0;}int count(){return ones;}string toString(){string value;for(bool bit:bits)value+=bit^flipped?'1':'0';return value;} };
`}}],c={id:2166,slug:i,titleEn:e,difficulty:"中等",category:"字符串",officialTags:t,paidOnly:!1,sourceUrl:n,title:s,studyTags:o,summary:l,insight:p,pitfalls:d,solutions:f};export{a as category,c as default,b as difficulty,r as id,p as insight,t as officialTags,u as paidOnly,d as pitfalls,i as slug,f as solutions,n as sourceUrl,o as studyTags,l as summary,s as title,e as titleEn};
