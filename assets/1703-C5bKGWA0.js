const u=1703,i="minimum-adjacent-swaps-for-k-consecutive-ones",e="Minimum Adjacent Swaps for K Consecutive Ones",m="困难",t="数组与矩阵",n=["数组","贪心","滑动窗口","前缀和"],c=!1,s="https://leetcode.cn/problems/minimum-adjacent-swaps-for-k-consecutive-ones/",d="得到连续 K 个 1 的最少相邻交换次数",o=["数组","贪心","滑动窗口","前缀和"],a="通过相邻交换让数组中至少有 k 个一连续，求最少交换次数。",r="记录一的位置 position[i]，减去其序号 i 后，目标连续位置的固定间隔被消去。任取 k 个连续的一，其最小移动代价是这些调整位置到中位数的绝对差和。",l=["选择的一在原序列中必是连续的一段","减去序号可免去奇偶 k 的额外间隔修正","绝对差和用 long 计算"],f=[{id:"adjusted-one-positions-median",title:"调整后位置中位数窗口",kind:"最优",idea:"把连续目标转换为所有调整坐标相等。",steps:["收集 adjusted[j]=onePosition-j，并建前缀和","枚举长度 k 的 adjusted 窗口，mid=(left+right)/2","用前缀和计算左右到 adjusted[mid] 的距离和，取最小"],complexity:{time:"O(n)",space:"O(number of ones)"},code:{java:`class Solution { public int minMoves(int[] nums,int k){List<Long> adjusted=new ArrayList<>();for(int i=0;i<nums.length;i++)if(nums[i]==1)adjusted.add((long)i-adjusted.size());long[] prefix=new long[adjusted.size()+1];for(int i=0;i<adjusted.size();i++)prefix[i+1]=prefix[i]+adjusted.get(i);long answer=Long.MAX_VALUE;for(int left=0;left+k<=adjusted.size();left++){int right=left+k-1,mid=(left+right)/2;long median=adjusted.get(mid);long cost=median*(mid-left)-(prefix[mid]-prefix[left])+(prefix[right+1]-prefix[mid+1])-median*(right-mid);answer=Math.min(answer,cost);}return (int)answer;} }
`,kotlin:`class Solution { fun minMoves(nums:IntArray,k:Int):Int{val adjusted=mutableListOf<Long>();for(i in nums.indices)if(nums[i]==1)adjusted.add(i.toLong()-adjusted.size);val prefix=LongArray(adjusted.size+1);for(i in adjusted.indices)prefix[i+1]=prefix[i]+adjusted[i];var answer=Long.MAX_VALUE;for(left in 0..adjusted.size-k){val right=left+k-1;val mid=(left+right)/2;val median=adjusted[mid];val cost=median*(mid-left)-(prefix[mid]-prefix[left])+(prefix[right+1]-prefix[mid+1])-median*(right-mid);answer=minOf(answer,cost)};return answer.toInt()} }
`,cpp:`class Solution { public: int minMoves(vector<int>& nums,int k){vector<long long> adjusted;for(int i=0;i<(int)nums.size();++i)if(nums[i])adjusted.push_back(i-adjusted.size());vector<long long> prefix(adjusted.size()+1);for(int i=0;i<(int)adjusted.size();++i)prefix[i+1]=prefix[i]+adjusted[i];long long answer=LLONG_MAX;for(int left=0;left+k<=(int)adjusted.size();++left){int right=left+k-1,mid=(left+right)/2;long long median=adjusted[mid];long long cost=median*(mid-left)-(prefix[mid]-prefix[left])+(prefix[right+1]-prefix[mid+1])-median*(right-mid);answer=min(answer,cost);}return answer;} };
`}}],g={id:1703,slug:i,titleEn:e,difficulty:"困难",category:t,officialTags:n,paidOnly:!1,sourceUrl:s,title:d,studyTags:o,summary:a,insight:r,pitfalls:l,solutions:f};export{t as category,g as default,m as difficulty,u as id,r as insight,n as officialTags,c as paidOnly,l as pitfalls,i as slug,f as solutions,s as sourceUrl,o as studyTags,a as summary,d as title,e as titleEn};
